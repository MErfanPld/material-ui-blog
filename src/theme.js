import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#34568B",
    },
  },
  backgroundColor: {
    default: "#FFFFFF",
  },
});

export default theme;
